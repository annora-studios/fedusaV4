# FEDUSA Congress Management Platform v5.3.7

## Scanner layout fix

- Removed the html5-qrcode built-in scan box so it no longer creates a second white scanner frame.
- Forced the camera video and decoding canvas to remain inside the intended camera stage.
- Retained the custom green scanner frame and scan line as the only visible overlay.
- Improved mobile sizing so the camera no longer renders above the scanner interface.
